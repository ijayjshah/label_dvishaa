{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "dev": "concurrently -k -n api,web -c cyan,magenta \"pnpm run dev:api\" \"pnpm run dev:web\"",
    "dev:api": "dotenv -e .env -- pnpm --filter @workspace/api-server dev",
    "dev:web": "dotenv -e .env -- pnpm --filter @workspace/label-dvisha dev",
    "db:push": "dotenv -e .env -- pnpm --filter @workspace/db push",
    "db:seed": "dotenv -e .env -- pnpm --filter @workspace/db run seed",
    "db:seed:admin": "dotenv -e .env -- pnpm --filter @workspace/db run seed:admin",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv-cli": "^11.0.0",
    "prettier": "^3.8.1",
    "turbo": "^2.9.10",
    "typescript": "~5.9.2"
  }
}
